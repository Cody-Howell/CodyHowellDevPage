import { Content } from "../../_WikiData";

export const JavascriptREADME: Content = {
  primaryCode: [
    '/**',
    '* @param {Array<Object>} data - Array of data',
    '* @param {string} filter - Value to compare',
    '* @param {Boolean} ascBool - True: Z-A',
    '*/',
  ],
  paragraphs: [
    `A bit of a placeholder page. This language will probably be a lot of libraries and style guides.`,
    `The above code is JSDoc notation; it's within a comment, and when you write your function, it 
    displays that information so you can check you're passing in the right parameters (assuming 
    your text editor handles JSDoc information). `
  ],
  seeSimilar: [],
  externalLinks: []
}